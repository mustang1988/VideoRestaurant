import { describe, it } from 'mocha';
import assert from 'assert';
import { MediaStreams } from '../../src/media/MediaStreams';

describe('MediaStreams.ts', () => {
    it.skip('getVideoStream()', () => {
        console.error('TODO add test case');
    });

    it.skip('getAudiiStream()', () => {
        console.error('TODO add test case');
    });
});
